package br.com.toponesystem.thirdsector.municipality.domain.model;

/**
 * @deprecated Substituido pelo modulo {@code plan} com tabela configuravel.
 * Mantido apenas como referencia de migracao — sera removido em limpeza futura.
 */
@Deprecated(forRemoval = true)
public enum Plan {
    BASIC, INTERMEDIATE, ENTERPRISE
}
